import { AppShopValidators } from './app-shop-validators';

describe('AppShopValidators', () => {
  it('should create an instance', () => {
    expect(new AppShopValidators()).toBeTruthy();
  });
});
